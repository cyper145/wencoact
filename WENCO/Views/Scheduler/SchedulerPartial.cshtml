@model WENCO.Model.SchedulerDataSource

@{
    var scheduler = Html.DevExpress().Scheduler(WENCO.Model.SchedulerHelper.Settings);
    if(ViewBag.GeneralError != null) {
        scheduler.SetErrorText(ViewBag.GeneralError);
    }
}
@scheduler.Bind(Model.Appointments, Model.Resources).GetHtml()